#!/bin/bash

trid=$(uuidgen)
trdir=reports/$trid

[ -z "$radish_opts" ] && radish_opts=--junit-xml=$trdir/$trid.junit.xml

mkdir -p $trdir
echo *** starting testrun "$trid"
echo *** running radish $radish_opts $FEATURES
radish $radish_opts $FEATURES
